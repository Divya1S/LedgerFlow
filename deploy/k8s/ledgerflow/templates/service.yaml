apiVersion: v1
kind: Service
metadata:
  name: {{ include "ledgerflow.fullname" . }}
  labels: {{- include "ledgerflow.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector: {{- include "ledgerflow.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
